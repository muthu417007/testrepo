import filterHeader from '@salesforce/label/c.BWC_Search_Facets_filtersHeader';
export const Labels = {
    /**
     * A label for the button to open the modal search filters UI - reads 'Filters'
     * @type {string}
     */
    filterHeader,
};